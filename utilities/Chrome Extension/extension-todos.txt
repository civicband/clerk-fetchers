Completed:
- Make skeleton for extension
- Add placeholder text to skeleton
- Add icon for extension
- Test extension locally
- Compile list of supported backends
- Comment in TODOs for each backend so I've got a spot to hold indicators
- Write instructions for requesting inclusion of a site on a supported backend
- Write issue format for a custom scraper request
- Test CivicClerk and AgendaCenter
- Make sure both sets of text tell users to check for their municipality before requesting
- Write fallback case where users whose site doesn't match anything can request it
- Find indicators for most supported backends

Outstanding:
- Find indicators for the last few supported backends
- Test all the other ones that should work so far
- [Repeat until all covered scrapers are addressed] Write detection for supported backend that shows users text about how to request if it's a match

Stretch: 
- If a user uses the extension for a site that's already being scraped, tell them the URL instead of giving them the prompt
- Would be nice to give the user a copy-pasteable issue filled out with the info we need to add the site if we don't have it yet